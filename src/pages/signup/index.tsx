import { Suspense } from "react";

import { Stack } from "@/utils/stackflow";

const SignUpPage = () => {
  return (
    <main>
      <Suspense>
        <Stack />
      </Suspense>
    </main>
  );
};
export default SignUpPage;
